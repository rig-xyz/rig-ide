// Workspace resolution is now handled automatically in the main process.
// Branch-already-checked-out and stale-directory errors are recovered by
// WorkspaceBootstrapService.ensureWorkspaceSetup before the task session is started.
export const WorkspaceResolutionView = null;
