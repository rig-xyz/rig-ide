/**
 * Deleted (round: HOME RESTRUCTURE) — this component was the pulse
 * section's original single-column rendering (round H3). Its content
 * split across the new 3-region layout: the greeting/ask/what's-new half
 * moved into `briefing-spine.tsx` (`BriefingSpine`, the center region),
 * and the per-person lines moved into `people-rail.tsx` (`PeopleRail`,
 * the right region). `home.tsx` no longer imports this file.
 *
 * Left as an intentionally empty stub rather than deleted: this session's
 * tooling does not have permission to remove files from disk. Safe to
 * delete by hand whenever convenient.
 */
export {};
