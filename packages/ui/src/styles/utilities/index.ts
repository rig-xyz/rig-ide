/**
 * styles/utilities — atomic CSS utilities for @emdash/ui.
 *
 * Re-exports:
 *   sx        — Sprinkles function for type-safe atomic class generation
 *   cx        — dependency-free class joiner (replaces clsx/cn)
 *   row, rowBetween, stack, center, fill, truncate, overlay — layout utilities
 */

export { sx } from './sprinkles.css';
export { cx } from './cx';
export { row, rowBetween, stack, center, fill, truncate, overlay } from './layout.css';
